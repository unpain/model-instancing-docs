# <span class="api-title">pauseAnimation()</span>

- **类别**：动画
- **签名**：`pauseAnimation(): void`

暂停动画时间推进，保留当前采样进度。

**示例**
```ts
primitive.pauseAnimation()
```

**注意**
- 与 `playAnimation()` 配合切换播放状态，不会修改实例动画索引或时间。


