# <span class="api-title">getLoopTransitionDuration()</span>

- **类别**：动画
- **签名**：`getLoopTransitionDuration(): number`

返回当前循环过渡时长设置（秒）。

**示例**
```ts
console.log(primitive.getLoopTransitionDuration())
```

**注意**
- 若未修改，默认值为 0.3。


